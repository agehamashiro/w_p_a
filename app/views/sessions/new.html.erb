<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8" />
  <title>ログイン</title>
  <link href="https://fonts.googleapis.com/css2?family=Kaisei+Decol&display=swap" rel="stylesheet" />
  <script src="https://cdn.tailwindcss.com"></script>
  <style>
    body {
      font-family: 'Kaisei Decol', serif;
    }
    .label-container {
      display: flex;
      flex-direction: column;
      align-items: center;
      max-width: 100%;
      overflow: hidden;
    }
    label {
      font-size: clamp(12px, 1.5vw, 24px);
      max-width: 100%;
      white-space: nowrap;
      overflow: hidden;
      text-overflow: clip;
      display: block;
    }
    input {
      width: 100%;
      border: 1px solid #ccc;
      border-radius: 6px;
      padding: 10px;
      font-size: 1rem;
    }
  </style>
</head>
<body class="min-h-screen bg-gray-50 flex justify-center pt-0 px-0">

  <div class="mx-auto w-full max-w-md bg-white border-4 border-[#800020] rounded-none shadow-2xl px-8 py-10 text-center space-y-10">

    <h1 class="text-5xl text-gray-800 tracking-wide">ワインペアリング提案 ログイン</h1>

    <%= form_with url: login_path, local: true, class: "space-y-6 text-gray-700" do |f| %>
      <div class="label-container">
        <%= f.label :email, "メールアドレス", class: "block mb-2 text-xl" %>
        <%= f.email_field :email, autofocus: true, required: true, class: "w-full border border-gray-300 rounded-md p-3" %>
      </div>

      <div class="label-container">
        <%= f.label :password, "パスワード", class: "block mb-2 text-xl" %>
        <%= f.password_field :password, required: true, class: "w-full border border-gray-300 rounded-md p-3" %>
      </div>

      <div>
        <%= f.submit "ログイン", class: "bg-red-700 hover:bg-red-800 text-white font-semibold py-3 px-8 rounded-full shadow-md transition" %>
        <%= link_to "Googleでログイン", "/auth/google_oauth2", class: "text-gray-700 hover:text-red-500" %>
      </div>
    <% end %>

    <p class="
