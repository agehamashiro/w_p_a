<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8" />
  <title>ログイン</title>
  <link href="https://fonts.googleapis.com/css2?family=Kaisei+Decol&display=swap" rel="stylesheet" />
  <script src="https://cdn.tailwindcss.com"></script>
  <style>
    body {
      font-family: 'Kaisei Decol', serif;
    }
    .label-container {
      display: flex;
      flex-direction: column;
      align-items: center;
      max-width: 100%;
      overflow: hidden;
    }
    label {
      font-size: clamp(12px, 1.5vw, 24px);
      max-width: 100%;
      white-space: nowrap;
      overflow: hidden;
      text-overflow: clip;
      display: block;
    }
    input {
      width: 100%;
      border: 1px solid #ccc;
      border-radius: 6px;
      padding: 10px;
      font-size: 1rem;
    }
  </style>
</head>
<body class="min-h-screen bg-gray-50 flex justify-center pt-0 px-0">

  <div class="mx-auto w-full max-w-md bg-white border-4 border-[#800020] rounded-none shadow-2xl px-8 py-10 text-center space-y-10">

    <h1 class="text-5xl text-gray-800 tracking-wide">ログイン</h1>

    <%= form_with url: login_path, local: true, class: "space-y-6 text-gray-700" do |f| %>
      <div class="label-container">
        <%= f.label :email, "メールアドレス", class: "block mb-2 text-xl" %>
        <%= f.email_field :email, autofocus: true, required: true, class: "w-full border border-gray-300 rounded-md p-3" %>
      </div>

      <div class="label-container">
        <%= f.label :password, "パスワード", class: "block mb-2 text-xl" %>
        <%= f.password_field :password, required: true, class: "w-full border border-gray-300 rounded-md p-3" %>
      </div>

      <div>
        <%= f.submit "ログイン", class: "w-full flex items-center justify-center border-2 rounded-full py-2 px-4 bg-white text-black font-semibold shadow-sm transition", style: "border-color: #800020;" %>

    </div>
    
    <div class="my-6 flex items-center">
      <div class="flex-grow border-t border-gray-300"></div>
      <span class="mx-4 text-gray-500">または</span>
      <div class="flex-grow border-t border-gray-300"></div>
    </div>
    
    <div class="flex justify-center">
      <%= link_to "/users/auth/google_oauth2", class: "w-full flex items-center justify-center border-2 rounded-full py-2 px-4 bg-white shadow-sm hover:bg-gray-100 transition", style: "border-color: #102e6d;" do %>
        <img src="/google.svg" alt="Google" class="h-6 w-6 mr-2">
        <span class="text-gray-800 text-base font-semibold" style="font-family: 'Kaisei Decol', serif;">Google でログイン</span>
      <% end %>
    </div>
    <% end %>
